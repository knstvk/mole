apply plugin: 'java'
apply plugin: 'idea'

sourceSets {
    main {
        java { srcDir 'src' }
        resources { srcDir 'src' }
    }
    test {
        java { srcDir 'test' }
        resources { srcDir 'test' }
    }
}

jar {
    manifest {
        attributes("Main-Class": "mole.Main")
    }
}